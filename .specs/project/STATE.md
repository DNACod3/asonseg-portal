# State

**Last Updated:** 2026-07-10
**Current Work:** **Fase 7 — Fachada Pública (Landing + Casca de Navegação) — COMPLETA, PASS** (AD-025). Rodada única (1 PR) em 3 USPs pelo pipeline Planner→Implementer→Verifier, cada USP com Verifier independente + mutação ao vivo, 0 fix→re-verify. **USP-046** casca do grupo `(public)` (Header+Nav+Footer em `src/app/(public)/_components/`, montada em `(public)/layout.tsx`; `<main>` duplicado removido de 5 rotas públicas; token novo `--color-footer`; 4 must-nots CASCA-MN-01..04). **USP-047** home/landing fiel ao protótipo (`#page-home`): 7 componentes de seção `home-*.tsx` (hero+busca+destaques+institucional+CTAs) compondo `(public)/page.tsx`, com os **indicadores reais da USP-041 embutidos no hero** (`HomeIndicatorsView` intacto, `revalidate=600`/`loadIndicators` preservados, contrato count-only/"Em breve"<5/PII-free mantido). **USP-048** navegação integrada: destaques de **vagas ACTIVE reais anonimizadas** via `searchJobs(…, viewer=null)` (rejeitado `listActivePublishedJobs` por vazar `nomeFantasia`/pular o gate), categorias reais via `listServiceCategories` → `/servicos?categoria=…` (fallback `/servicos`), CTAs de empresa → `/empresa/cadastrar`, busca GET → `/vagas?q=` (já lida pela USP-021); guard novo `nav-no-dead-ends` (sem `href="#"`). **Sem migração, sem query nova** (reusa USP-021/030). Branch `feat/fase-7-fachada-publica`. Gates no HEAD `4e80af7`: typecheck/lint verdes, **1635 unit** (244 arquivos), `NODE_ENV=production` build OK (`/` prerender estático `revalidate:10m`, resiliente a DB indisponível), zero migração. ROADMAP: USP-046/047/048 `[x]`; Fase 7 100% — **MVP feature-complete E com fachada pronta**. Próximo: **Lançamento** (UAT/cutover; B-001 resolvido em AD-024, resta formalização do DPO + B-003/B-004); abrir/mergear o PR único.

**Hist. imediato:** **Fase 6 — Relatórios + Home + Hardening + LGPD — COMPLETA, PASS** (AD-023). Rodada única (1 PR) em 3 unidades pelo pipeline Planner→Implementer→Verifier, cada USP/unidade com Verifier independente + mutação ao vivo. **U1** USP-041 (home com indicadores MP4/MP1/MP2 count-only, "Em breve" <5, ISR 600s + revalidate on-status→ACTIVE; **funda o módulo `reporting`**) + USP-042 (5 relatórios operacionais + social-por-região, RBAC por guard de papel inline sem novo `PermissionId`, export CSV com marca d'água LGPD + PDF `@react-pdf/renderer`, evento `REPORT_EXPORTED`, privacidade 2-barreiras da ficha ao coordenador). **U2** USP-044 (dispatcher assíncrono do Outbox: cron GET `/api/cron/dispatch-outbox` fail-closed, drain com `FOR UPDATE SKIP LOCKED` sem double-send, template novo `job-expiry` + resolver `jobs` via DI, poison-cap 5, fluxos de auth seguem síncronos; **sem migração**). **U3** (ad-hoc, não-USP) hardening: CAPTCHA adaptativo no login (≥3 falhas), headers de segurança em `/api`, guard estático de Server Actions, redação de PII no logger + guard anti-`console.*`, piso de flags de cookie de sessão. Branch `feat/fase-6-relatorios-home-hardening`. Gates no HEAD: typecheck/lint verdes, **1518 unit, 612 integração** (suite inteira verde em DB limpo), `NODE_ENV=production` build OK, **zero migração nova**, 36 commits. Corrigido de passagem 1 flake de fuso pré-existente na fixture `validUntil` (test-only, classe L-006). ROADMAP: USP-041/042/044 `[x]`; Fase 6 100% — **MVP feature-complete**. Próximo: **Lançamento** (UAT/cutover, gate humano B-001 DPO); abrir/mergear o PR único. **Hist. imediato:** **Fase 5 — Ficha Social + Encaminhamento + Visão Consolidada — COMPLETA, PASS** (AD-022). Rodada única (1 PR) planejada→implementada→validada em 3 unidades pelo pipeline Planner→Implementer→Verifier, cada USP com Verifier independente e teste de mutação ao vivo. **U1** USP-036 ficha socioeconômica (módulo `persons`, model novo `SocioeconomicRecord` 1:1 com Person, enums `IncomeBracket`/`HousingSituation`, guard inline `canManageSocioeconomicRecord` = SOCIAL_ASSISTANT/BOARD, criptografia-em-repouso = plataforma gerenciada Supabase por precedente ADR-0012 — cripto de coluna deferida à arquitetura/diretoria; must-nots SOC-036-MN-01 vazamento de campo sensível / MN-02 write sem auditoria). **U2** USP-037+038 encaminhamento (módulo novo `referrals`: model `Referral` com colunas de resultado já nullable + enum `ReferralResult`, FK `Application.viaReferralId @unique` + flip `viaEncaminhamento`, RBAC `REFER_PERSON_TO_JOB`/`REGISTER_REFERRAL_RESULT` já no catálogo, consentimento tácito `SOCIAL_REFERRAL_TO_JOB` via novo helper `ensureCandidateRole`, revalidação vaga-ATIVA dentro da tx, resumo profissional obrigatório quando sem CV, e-mail no Outbox `referral-notification`, badge "Encaminhado pela ASONSEG"; 7 must-nots incl. unicidade sob concorrência exercitando o índice real do DB — L-010). **U3** USP-039 visão consolidada (View Model `viewPersonForSocialAssistant` em `persons`, rota `(app)/pessoas/[id]/visao-consolidada`, padrão composition-root p/ evitar ciclo de import AD-019, 4 reads novos por dimensão, omissão da ficha ao coordenador em 2 barreiras — nunca faz SELECT do sensível p/ não-AS/BOARD, voluntário negado; must-nots SOC-039-MN-01/02). Branch `feat/fase-5-ficha-social-encaminhamento`. Gates finais no HEAD `97f772d`: typecheck/lint verdes, **1354 unit, 561 integração**, `NODE_ENV=production` build OK, E2E dos gates de sessão/RBAC verdes, 2 migrações novas aplicam limpas (`usp036_socioeconomic_record`, `usp037_referral`). ROADMAP: USP-036/037/038/039 marcadas `[x]`; Fase 5 100%. Próximo: abrir/mergear o PR único; **Fase 6 (USP-041 home com indicadores)** a seguir. **Hist. imediato:** **Remediação da review do PR #284 — COMPLETA, PASS** (AD-021). Os 7 achados da review multi-agente corrigidos na branch `feat/fase-4-servicos-manifestacoes` pelo pipeline Planner→Implementer→Verifier; gates verdes no HEAD `1e6053f` (1252 unit, 495 integração, build OK), Verifier independente PASS com mutação ao vivo. Commits locais — pendente push do branch + resposta aos comentários inline. **Hist. imediato:** **Fase 4 — Serviços + Manifestações — COMPLETA, PASS** (AD-020). Rodada única (1 PR) planejada→implementada→validada em 3 unidades pelo pipeline Planner→Implementer→Verifier, cada USP com Verifier independente. **U1** cadastros de papel: **USP-010** (prestador, já implementada) refatorada ao Design System (AD-014, style-only, guard `PRV-MN-01`) + **USP-011** (cliente) reconciliada (já implementada via `ensureClientRole` — sem UI self-service, papel nasce na 1ª manifestação). **U2** serviços (**módulo novo `services`**, espelho de `jobs`): **USP-029** publicar (model `Service`+`ServicePhoto`, FSM `ContentKind.SERVICE`, `PrismaServiceStatusRepository` no container, upload de foto MIME real ≤5MB/×3, gate = papel PROVIDER + consent `SERVICE_OFFERING`, **sem** gate de `company.isVerified`), **USP-030** busca pública (unaccent/trgm, on-read ACTIVE + prestador ativo, ISR), **USP-031** detalhe (`viewServiceDetail` fonte única + JSON-LD, **contato do prestador oculto de todos** — defesa em 3 camadas: SELECT + View Model explícito + tipo sem campo), **USP-032** editar/pausar/arquivar (FSM, `editService` exceção atômica). **U3** manifestações (`ServiceInterest` em `services`): **USP-033** manifestar (auto-ativa papel CLIENT na mesma tx auditada via `ensureClientRole`, consent `SERVICE_HIRING` com cascade, índice único parcial `uq_service_interest_active`, e-mail no Outbox `service-interest-notification`, **revela contato do prestador ao cliente com interesse ativo**), **USP-034** cancelar (owner-scoped, idempotente, re-manifestar após cancelar), **USP-035** prestador ver manifestações (owner-scoped, `viewClientForProvider`, audit-on-read). Branch `feat/fase-4-servicos-manifestacoes`. Gates finais no HEAD: typecheck/lint verdes, **1237 unit, ~486 integração**, `NODE_ENV=production` build OK, E2E do gate de sessão verde, 3 migrações novas aplicam limpas (`usp029_service`, `usp030_service_search`, `usp033_service_interest`). U3 teve 1 fix→re-verify (sensor de concorrência `SVC033-MN-03` estava mascarado por pré-check da app; fortalecido p/ exercitar o índice do DB direto — 8/8 red com índice dropado, 8/8 green restaurado). ROADMAP: USP-011/029/030/031/032/033/034/035 marcadas `[x]`; Fase 4 100%. Próximo: **Fase 5 (USP-036 ficha socioeconômica)**.

**Hist. anterior:** **Fase 3 — Candidaturas + Busca + Extração de CV — COMPLETA, PASS** (AD-019). Rodada única (1 PR) planejada→implementada→validada em 4 unidades pelo pipeline Planner→Implementer→Verifier, cada USP com Verifier independente. **USP-009** (já implementada) foi **refatorada** ao Design System (AD-014, style-only 2 telas — U1). 5 USPs **novas** implementadas: **U2** candidaturas (025 candidatar-se, 026 cancelar — agregado `Application` write path, AD-017); **U3** busca de candidatos (027 ver candidatos da vaga, 028 busca ativa — View Models `viewCandidateForEmployer`/`viewCandidateForSearch`, privacidade 2-barreiras, AD-018) + fix `Person.phone` persistido no cadastro de candidato; **U4** extração de CV via IA (040 — módulo novo `cv-extraction`, porta `CVExtractor` + adapter Anthropic + fake, upload Storage com MIME real, rate limit durável, confirmação humana obrigatória antes de persistir). Branch `feat/fase-3-candidaturas-busca-cv`. Gates finais no HEAD: typecheck/lint verdes, **1121 unit, 385 integração**, `NODE_ENV=production` build OK, E2E verde, 2 migrações novas aplicam limpas (`usp025_applications_write`, `usp028_candidate_search`, `usp040_cv_upload_attempts`). U2 teve 1 re-verify (Verifier morreu por erro de API a meio da mutação; tree restaurada + re-rodada limpa). ROADMAP: USP-025/026/027/028/040 marcadas `[x]`; Fase 3 100%. Próximo: abrir o PR único; Fase 4 (USP-011 cadastro de cliente) a seguir.

**Hist. anterior:** **Fase 2 — Empresas + Vagas + Moderação — COMPLETA, PASS** (AD-016), 13 USPs em 1 PR único.

**Hist. anterior:** **Fase 1 — refactor amplo (design system + consistência) — COMPLETO, PASS** (AD-015), mergeada no #281.

**Hist. anterior:** USP-022 (Ver detalhe da vaga, #172) — KICKOFF (Spec/design/tasks em `.specs/features/vagas/usp-022-detalhe-vaga/`, 4 tasks, AD-012).

**Hist. anterior:** USP-017 (Validar Empresa na 1ª vaga, #155) — **DEV COMPLETO** (não commitado). #156 (backend) + #157 (UI) implementados. Gates verdes: typecheck ✓, lint ✓, 665 unit ✓, 192 integração ✓ (Postgres local). **Pendente:** branch/PR + protocolo OpenWolf de fechamento. ⚠️ Mudanças estão na working tree do branch `feat/usp-020-publicar-vaga` (USP-020 ainda NÃO mergeada em master) — USP-017 depende do model `Job` da USP-020, então a PR de USP-017 deve **empilhar** sobre a branch da USP-020 (ou ser desenvolvida após o merge da 020).

**Resumo da entrega USP-017:** #156 — migração `Company` (+`verifiedAt/By/JobId`, `verifiedSnapshot Json`, `rejectionCount`); `PrismaCompanyVerifyHook` real (substitui o stub) com `onContentActivated` (verifica 1ª vaga: marca+snapshot dentro do tx + `COMPANY_VERIFIED`) e `onContentRejected` (incrementa contador); snapshot lido dentro do tx (P-004); idempotência via `isVerified` (E-004/AD-2); `recordAuditEvent` extraído do `withAudit` p/ evento secundário no mesmo tx; guard estático P-005/D-004 (`no-external-verify.test.ts`). #157 — `viewModerationQueue` une vagas reais (`jobs`) ao fixture e popula `companyUnverified`/`companyId`; `CompanyVerificationContext` (View Model + diff D-006); `listCompanyRejections` (histórico via audit_log); `VerificationPanel` (banner, dados, checklist P-001 que bloqueia aprovar, separação P-002, histórico P-003, estado verificado E-004, diff D-006) integrado à `ModerationQueue` + página. Checklist com itens de fonte configurável (`verification-checklist.ts`, R3); conteúdo definitivo = D-001 (gate de produção).

**Histórico USP-013:** concluída e mergeada (backend #266/#268, UI #131/#268). Schema `status`, invariante, outbox/e-mail entregues.

**Histórico recente:** USP-016 (Moderar rascunho, #117) concluída e mergeada (fundação `moderation`).

**Histórico USP-009:** (Cadastro de candidato, #31) concluída (backend já entregue via `moderation.transitionContent`; refatorada ao DS na Fase 3 U1) e `Person.phone` passou a ser persistido (fix da Fase 3 U3). Não está mais parada.

---

## Handoff

**Fase 8 — Remediação do UAT em andamento** (branch `feat/fase-8-remediacao-uat`, 1 PR único, pipeline Planner→Implementer→Verifier por USP). Concluídas: USP-049..053 (PASS). Próxima unidade: **USP-054 — Ciclo de vida da vaga no painel** (`.specs/features/ajustes-uat/usp-054-ciclo-vida-vaga/`).

---

## Recent Decisions (Last 60 days)

### AD-025: Fase 7 — Fachada Pública (Casca + Home + Navegação integrada), rodada única em 1 PR — PASS 2026-07-10

**Decision:** Executar a **Fase 7 inteira** (3 USPs net-new, fora das 44 do PRD) em **um único PR**, a pedido do dono ("execute a fase 7"), planejada→implementada→validada pelo pipeline Planner→Implementer→Verifier (modelo AD-016/019/020/022/023), execução autônoma, cada USP com Verifier independente + mutação ao vivo. **Unidades sequenciais** (deps internas): USP-046 (casca) → USP-047 (home) → USP-048 (nav integrada). Decisões técnicas:
- **USP-046 — Casca de navegação pública.** Header+Nav+Footer como Server/Client Components em **`src/app/(public)/_components/`** (NÃO `shared/ui`, reservado a primitivas genéricas; `_`-prefix = pasta privada do Next, mantém `src/` fechado) — **esta é a decisão de localização de componente de rota da fase**. Montada em `(public)/layout.tsx` (envolve só o grupo público; `(auth)`/`(app)` têm sua própria casca) **sem** declarar `revalidate` (ISR por-página preservado). Nav data-driven (`PublicNav` com `NavItem[]` + seam `actions`), links reais `/`,`/vagas`,`/servicos`,`/login`,`/cadastro`, active-state via `usePathname`, menu mobile acessível (`useState` + SVG inline, sem lib de estado/ícone). Removido `<main>` duplicado de 5 rotas públicas (o layout passa a prover o único `<main>`). Token novo `--color-footer` (footer fixo-escuro nos 2 temas, sem hex em `.tsx`). Must-nots **CASCA-MN-01** (casca não importa sessão/`getCurrentPerson`/View-Model/Prisma/action nem renderiza PII — chrome público estático), **MN-02** (tokens-only, sem hex/paleta fixa), **MN-03** (sem CDN/fonte externa, DS-MN-01), **MN-04** (sem lib de estado/`lucide-react`). Guards estáticos `casca-*` varrem recursivamente `(public)/_components/**` → cobrem automaticamente os componentes das USP-047/048.
- **USP-047 — Home/landing fiel ao protótipo.** Reconstrói `(public)/page.tsx` reproduzindo `#page-home` do protótipo em **7 componentes de seção** `home-*.tsx` (hero, busca, destaques de vagas/serviços, "como funciona", personas, serviços, CTA) — todos Server Components tokens-only. **Estende — não descarta — os indicadores da USP-041**: os fake hero-stats do protótipo dão lugar ao `HomeIndicatorsView` real, embutido no hero; `revalidate=600`+`loadIndicators()`+`FALLBACK_INDICATORS` preservados verbatim; contrato count-only/"Em breve"<5/PII-free/ISR≤600 mantido (must-not HOME-MN-03). HOME-MN-04: layout provê o único `<main>`, hero provê o único `<h1>`. `page.test.tsx` migrado (o `<h1>` do esqueleto vira o do hero).
- **USP-048 — Navegação integrada.** Preenche os seams da USP-047 **sem query nem migração nova** (reusa USP-021/030): destaques = **top-2 vagas ACTIVE reais** via `searchJobs({page:1}, null)` — `viewer=null` → `company.displayName` anonimizado, nunca `nomeFantasia`; **rejeitado `listActivePublishedJobs`** por vazar `nomeFantasia` e pular o gate ACTIVE. Categorias reais via `listServiceCategories` → `/servicos?categoria=<id>` (match por nome normalizado NFD, **fallback determinístico `/servicos`** sem id inventado, lições L-002/L-005). CTAs de empresa → `/empresa/cadastrar` (rota real; `requireActivePerson` redireciona anon a `/login` por design), candidato → `/cadastro`. Busca da home = form GET nativo → `/vagas?q=` (já lido pela USP-021, `vagas/page.tsx`), inalterada. Wiring vive no composition-root `page.tsx` (só barrels `@/modules/*`, permitidos pelos guards; os `home-*.tsx` seguem apresentacionais com seams `href?` retrocompatíveis). Loaders espelham o try/catch→fallback de `loadIndicators` (a home ainda faz build sem DB). Must-nots **NAV-MN-01** (destaques nunca com PII/não-ACTIVE/viewer real; `page.tsx` não importa o conjunto proibido) e **NAV-MN-02** (sem dead-ends `href="#"`, sem bypass de moderação, sem regressão de hex/CDN) — guard novo `nav-no-dead-ends.test.ts`. Correção de fato: default-param só dispara em `undefined`, então `loadFeaturedJobs` retorna `undefined` (não `[]`) no vazio/erro p/ o fallback-mock renderizar (mutação #4 do Verifier provou ser load-bearing).
**Reason:** Fecha o gap entre "features implementadas" (Fases 1–6) e "experiência do protótipo": a home saíra do esqueleto de inicialização da Fase 1 e o site ganhou casca global + navegação unificada. Precede o Lançamento (não se valida o MVP com a home no esqueleto). O dono pediu "execute a fase 7".
**Trade-off:** 3 USPs net-new (não estão no PRD/board, como USP-045) — numeração do ROADMAP, não do board. E2E autenticado/navegação deferido (padrão L-007; cobertura autoritativa em RTL + guards estáticos + build). Empresa CTA aponta a rota autenticada `/empresa/cadastrar` (não há cadastro público de empresa no MVP) — seam reapontável. Destaque de serviços fica category-based (protótipo não tem busca de serviços). Sem migração, sem dep nova.
**Impact:** 3 Verifiers independentes PASS, **0 fix→re-verify**. Sensores matando as mutações-alvo: import de auth/PII na casca (guard estático recursivo), hex cru em `.tsx`, CDN externa, lib de estado no menu mobile, 2º `<main>`/2º `<h1>`, ordem de seções, regressão do contrato de indicadores/ISR, dead-link default, **vazamento de `nomeFantasia`/viewer≠null no destaque** (decoy no mock provado killable), fallback `undefined`-vs-`[]`, id de categoria inventado. Gates no HEAD `4e80af7`: typecheck/lint verdes, **1635 unit (244 arquivos)**, build `NODE_ENV=production` OK (`/` prerender estático `revalidate:10m`), zero migração. Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/fachada-publica/usp-04{6,7,8}-*/`. Lições candidatas L-013/L-014 (hardening dos guards estáticos: regex de import/hex cega a formas bare/side-effect). **Fase 7 = 100%. Desbloqueia o Lançamento** (UAT/cutover), cujos gates humanos restantes são a formalização do DPO (B-001/AD-024) + o conteúdo das checklists (B-003/B-004).

### AD-024: Parâmetros LGPD resolvidos pelo dono (retenção 5 anos · esquecimento pós-piloto · DPO = Diretora Geral) — 2026-07-10

**Decision:** Em resposta aos flags da Fase 6 (AD-023), o dono decidiu:
- **Retenção de PII operacional = 5 anos.**
- **Direito ao esquecimento / anonimização a pedido do titular = NÃO agora — apenas depois do piloto.** Mantém ADR-0008 (inativar Pessoa preserva os dados) até lá.
- **DPO (encarregado LGPD) = a Diretora Geral** — o cargo, quem quer que o ocupe. **Resolve B-001.**
- **Métricas MP1-MP10:** home/relatórios seguem exibindo **valor corrente** (sem progresso-vs-meta) — confirmado como está (QP-007 encerrado do lado de produto).
- **Dep `@react-pdf/renderer`:** aceita.
**Reason:** Os deferidos LGPD da Fase 6 (DEF-3/DEF-4 do design de hardening) e o B-001 dependiam de decisão da diretoria/jurídico, não de engenharia. O dono forneceu os parâmetros.
**Trade-off:** A retenção de 5 anos e o esquecimento a pedido são a **mesma frente de deleção/anonimização de PII** e ficam juntos como **follow-up pós-piloto** — coerente com "esquecimento só depois do piloto" e com o fato de que um purge de 5 anos não dispara antes disso. **Parâmetros já fechados pelo dono (2026-07-10), a frente nasce com spec completa:** (a) **início do relógio dos 5 anos = a inativação da Pessoa** (`Person.status → INACTIVE`, provável coluna `inactivatedAt`/evento de auditoria como âncora); (b) **ao vencer, ANONIMIZA** (não deleção física — preserva os agregados dos relatórios MP; strip de PII in-place). A descoberta do `ANONIMIZAR` morto (AD-023) segue válida: essa frente terá de **construir os apliers de anonimização de fato** (hoje inexistentes) + um cron de varredura (padrão `auth-attempts-retention`) que anonimiza Pessoas inativas há ≥5 anos e cascateia às entidades com PII (CandidateProfile, SocioeconomicRecord, CV no Storage, dados de contato).
**Impact:** **B-001 resolvido** (go-live destravado do lado do DPO — falta só a formalização escrita/publicação do encarregado, tarefa operacional, não de dev). Nenhuma mudança no PR #286 (Fase 6): as decisões confirmam o que já foi entregue (valor-corrente, PDF) e o que ficou deferido (retenção/esquecimento). Follow-up registrado: **unidade de retenção+esquecimento de PII pós-piloto** (job de purge/anonimização de 5 anos + fluxo de esquecimento a pedido), agora desbloqueada de premissa.

### AD-023: Fase 6 — Relatórios + Home + Hardening + LGPD, rodada única em 1 PR — PASS 2026-07-09

**Decision:** Executar a **Fase 6** em **um único PR**, a pedido do dono ("3 USPs + unidade de hardening"), planejada→implementada→validada pelo pipeline Planner→Implementer→Verifier (modelo AD-015/016/019/020/022), execução autônoma, cada USP/unidade com Verifier independente + mutação ao vivo. **Unidades:** **U1** (`indicadores-relatorios`) USP-041 + USP-042 · **U2** (`notificacoes-email`) USP-044 · **U3** (ad-hoc, não-USP) hardening. Decisões técnicas:
- **U1 · USP-041** — home pública, 3 indicadores **count-only** (MP4 vagas ACTIVE, MP1 CandidateProfile ACTIVE, MP2 Company verificada); o tipo de retorno PII-free é a barreira estrutural do must-not (REL41-MN-01). "Em breve" quando <5 (REL41-MN-02). ISR `revalidate=600` (já existia) + on-demand `revalidatePath('/')` num **único chokepoint** `transitionContent(to=ACTIVE)` (ADR-0011). **Funda o módulo `reporting`** (`domain/metrics.ts` catálogo MP1-MP10, `queries/`, barrel). **Sem migração.** Fix pré-existente: `ACCESS_REPORT_ROLES` movido de arquivo `'use server'` p/ `domain/`.
- **U1 · USP-042** — 5 relatórios (R1 vagas/MP4, R2 candidaturas/MP6, R3 serviços+manifestações/MP5+MP7, R4 encaminhamentos/MP8+MP9, R5 fila/MP10+MP3) + R6 social-por-região. **RBAC = guard de papel inline** (`canViewOperationalReports`={COORDINATOR,BOARD}, `canViewSocialReports`={SOCIAL_ASSISTANT,BOARD}) **sem novo `PermissionId`** (precedente AD-022/USP-036); exceção: R5 reusa `MODERATE_*` delegável (P-006). Export **CSV** (serializer puro RFC-4180 + marca d'água LGPD, delimitador `;`) e **PDF** (`@react-pdf/renderer` — **única dep nova**). Evento `REPORT_EXPORTED` no catálogo (audit-fail → rollback, MN-07). **Privacidade 2-barreiras** da ficha ao coordenador (SELECT condicional ao papel — não vaza no payload Flight, MN-05). Rota + export compartilham 1 superfície de autorização (`report-authorization.ts`). MP9 sempre exibe "sem resultado registrado" junto (MN-04). 7 must-nots REL42-MN-01..07. **Sem migração de schema.**
- **U2 · USP-044** — dispatcher assíncrono do Outbox (o que AD-007 deferiu). Cron **GET `/api/cron/dispatch-outbox`** fail-closed (`verifyCronSecret`), entrada em `vercel.json` (`* * * * *`). **Concorrência (invariante central):** drain por linha em tx curta com **`SELECT … FOR UPDATE SKIP LOCKED`**, envio dentro do lock, `processedAt` na mesma tx → sem double-send sob drains concorrentes (U44-MN-01, exercita o lock real do DB — L-010, overlap forçado matou 12-vs-6). Batch 50, oldest-first, `attempts < MAX(5)` (poison nunca re-selecionado nem bloqueia o batch — U44-MN-03). **Payload misto:** 5 templates full `EmailMessage` passthrough; `{kind:'JOB_EXPIRY_D3',jobId}` hidratado por `resolveJobExpiryEmail` (owned por `jobs`, registrado via **DI token** p/ `shared` não importar `jobs`) → **template novo `job-expiry`**; entidade/destinatário ausente = no-op gracioso. **Fluxos de auth seguem síncronos** (register/reset/claim — D-1). Sem evento de auditoria novo (D-2). **Usa só colunas existentes do Outbox — sem migração.** 4 must-nots U44-MN-01..04.
- **U3 · hardening (5 itens, não-USP, flipa nenhum checkbox)** — **H1** CAPTCHA adaptativo no login (Turnstile exigido a partir de ≥3 falhas em `(email,ip)`, abaixo do lockout de 5; <3 = zero fricção; contrato de login mudou, testes-âncora atualizados; código `CAPTCHA_REQUIRED`). **H2** headers de segurança estendidos a `/api/**` (headers-only, sem rate-limit/redirect → cron sobrevive). **H3** guard estático exigindo gate de sessão/permissão em toda `'use server'` action (allowlist p/ públicas: login/register/reset/claim + `acceptRoleConsent` defendida por HMAC). **H4** logger redige PII de domínio (`fullAddress`/`endereco`/`birthDate`/`nascimento`/CV) + guard anti-`console.*` em módulos. **H5** piso de flags de cookie de sessão (HttpOnly/Secure-em-prod/SameSite; `none`→`lax`, sem rebaixar valores seguros). Must-nots MN-H1..H5 (+H4b).
**Reason:** Fecha a fase final do MVP: home pública com indicadores em tempo real (MP1/MP2/MP4 visíveis), relatórios operacionais de gestão (MP3-MP10, privacidade por papel), notificações por e-mail **enfim entregues de fato** (o Outbox acumulava 6 sites de enfileiramento sem consumidor desde a USP-013/AD-007), e endurecimento de segurança do MVP. O dono pediu "3 USPs + unidade de hardening".
**Trade-off:** PR grande (revisão pesada) — aceito. Dep nova `@react-pdf/renderer`. E2E autenticado limitado aos gates de sessão/RBAC (L-007); cobertura autoritativa nos testes de integração/componente. **Flags/deferidos ao dono (não bloqueiam dev):** (1) metas absolutas MP1-MP10 não confirmadas (QP-007/D-004) → home/relatórios mostram **valor corrente**, não progresso-vs-meta; (2) habilitar relatórios de PII em produção depende do sign-off do DPO (B-001), como B-003/B-004; (3) **LGPD deferido** — direito ao esquecimento/anonimização de Pessoa (ADR-0008 preserva por design), política de retenção de PII operacional, e cascade `ANONIMIZAR` → **gated por B-001** + apliers não construídos; **descoberta decisiva:** a camada `artifactEffects`/`ANONIMIZAR` de `revocation-cascade.ts` é **dado declarativo morto** (`revokeConsent` nunca lê a matriz; `ANONIMIZAR` mapeia p/ zero artefatos) — não foi "cabeada" porque não há alvo; (4) CSP nonce-based, rate-limit distribuído (Upstash), CAPTCHA por-IP → deferidos (infra/tamanho). **Correções de fato (o survey inicial errou):** **não existe** cron de retenção de `audit_log` 365d (só `auth-attempts-retention` 90d); "D-003" não existe no STATE.
**Impact:** 4 Verifiers independentes PASS, 0 fix→re-verify de conteúdo (o único incidente foi o Implementer da USP-042 parar sem commitar a UI dos relatórios — retomado por SendMessage, sem retrabalho). Sensores matando as mutações-alvo: vazamento de PII na home (count-only), "Em breve"<5, TTL≤600, marca d'água LGPD, fila gated por `MODERATE_*`, ficha stripped ao coordenador (2-barreiras), export gated por ciência + `REPORT_EXPORTED`, double-send sob concorrência (lock real 12-vs-6), cron fail-closed, poison-cap + isolamento, PII fora do log, CAPTCHA no brute-force sem chamar o provedor, headers em `/api`, guard de action gate-less, redação de PII, piso de cookie. Gates no HEAD: typecheck/lint verdes, **1518 unit, 612 integração** (suite inteira verde em DB limpo migrado do zero), `NODE_ENV=production` build OK, **zero migração nova**, 36 commits. **1 flake pré-existente corrigido de passagem** (fuso na fixture `validUntil` de apply-to-job/create-referral — `@db.Date` truncado em UTC vs hoje-SP empatava na janela 00:00–03:00 UTC; `-1`→`-2` dias; test-only, classe L-006). E2E de login 2/3 falham por **crash pré-existente do `next dev`** (worker thread; reproduzido no baseline via worktree; não-regressão; E2E gated no CI). Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/{indicadores-relatorios,notificacoes-email,fase-6-hardening}/`. **Fase 6 = 100%, MVP feature-complete.** Desbloqueia o **Lançamento** (UAT/cutover), cujo gate humano é **B-001** (designação do DPO) + os gates de conteúdo B-003/B-004.

### AD-022: Fase 5 — Ficha Social + Encaminhamento + Visão Consolidada, rodada única em 1 PR — PASS 2026-07-09

**Decision:** Executar a **Fase 5 inteira** (4 USPs) em **um único PR**, a pedido do dono, planejada→implementada→validada em 3 unidades pelo pipeline Planner→Implementer→Verifier (mesmo modelo de AD-015/016/019/020), execução autônoma, cada USP com Verifier independente + teste de mutação ao vivo. **Unidades:** **U1** USP-036 (ficha socioeconômica) · **U2** USP-037+038 (encaminhamento + resultado, agregado `Referral`) · **U3** USP-039 (visão consolidada). Decisões técnicas:
- **U1 (USP-036)** — model novo `SocioeconomicRecord` (tabela `socioeconomic_records`, 1:1 com `Person`, `onDelete: Cascade`) no módulo `persons`, seguindo AD-009 (status/dados-na-entidade, padrão `CandidateProfile`/`ProviderProfile`); enums `IncomeBracket`/`HousingSituation` (arrays literais locais no domain p/ não puxar runtime do Prisma ao Client Component — precedente `EDUCATION_LEVELS`). RBAC = **guard inline** `canManageSocioeconomicRecord(roles)` (SOCIAL_ASSISTANT/BOARD), **não** `PermissionId` de catálogo (capacidade intrínseca ao papel, não-delegável — diferente da USP-037). **Criptografia-em-repouso (AC-036-4) = plataforma gerenciada Supabase** (disk-level, precedente ADR-0012, mesmo tratamento de `candidate_profiles`/CV) — cripto de coluna/aplicacional explicitamente **deferida** pelo architecture-document §4.3, decisão de arquitetura/diretoria (**flag ao dono**, não bloqueia dev). `requireActiveConsent` = **N/A** (base legal legítimo interesse/mandato institucional; a própria edge case do épico exige referenciar Pessoa sem credencial — **flag ao dono**). Controles compensatórios: acesso restrito (MN-01, defesa em profundidade) + audit-on-read `SENSITIVE_FIELD_VIEWED`. Must-nots SOC-036-MN-01 (nenhum campo sensível SELECTado/serializado — incl. payload RSC/Flight — p/ viewer não-AS/BOARD; lição "anonimizar no View Model não basta") e MN-02 (nenhum write sem registro append-only de autor+data na mesma tx). Rota `(app)/pessoas/[id]/ficha-social` (reusa a família de rotas de gestão de Pessoa USP-007/045). Rota 8 tasks.
- **U2 (USP-037+038)** — **módulo novo `referrals`** (lista canônica dos 11, sem RFC; possui só a tabela `referrals`, orquestra persons+jobs na sua tx, importa `applyToJob`/helpers via barrel `@/modules/jobs`, precedente AD-017). Migração `usp037_referral` cria o agregado inteiro **incluindo as colunas de resultado já nullable** → USP-038 **sem migração** (só action+UI). Enum `ReferralResult {HIRED,NOT_SELECTED,UNDER_REVIEW,NO_RESPONSE}`. `Application` ganha `viaReferralId String? @unique` + relação; caminho de encaminhamento seta `viaEncaminhamento=true, viaReferralId=referral.id` (invariante `viaReferralId != null ⟺ viaEncaminhamento`), realizando o que AD-017 antecipou. **Unicidade:** mesma Pessoa→mesma vaga bloqueada enquanto há Application ativa (índice parcial existente `uq_application_active` → P2002 → CONFLICT → rollback atômico, sem `Referral` órfão); múltiplos encaminhamentos p/ vagas diferentes OK. RBAC `requirePermission('REFER_PERSON_TO_JOB')` (037) / `('REGISTER_REFERRAL_RESULT')` (038) — **ambas já no catálogo** (intrínsecas a COORDINATOR+SOCIAL_ASSISTANT, delegáveis). **Consentimento tácito** `SOCIAL_REFERRAL_TO_JOB` via novo helper `ensureCandidateRole(tx, …)` em `persons` (espelha `ensureClientRole`), idempotente, registra consent + `CANDIDATE_ROLE_ACTIVATED` + `CONSENT_GRANTED` na tx do encaminhamento; **não** faz gate de `PORTAL_ACCESS` → Pessoa sem credencial pode ser encaminhada (mandado pela edge case do épico — divergência de semântica de consentimento vs. `ensureClientRole`, **flag de review**). Revalidação vaga-ATIVA via `isJobOpenForApplication` up-front **e** dentro da tx (TOCTOU); resumo profissional obrigatório quando `CandidateProfile.cvStoragePath == null` (regra pura `isProfessionalSummaryRequired`, senão `VALIDATION` zero-rows); Application escrita via novo participante de tx `createReferralApplication` em `jobs` (os gates de sessão/consent/perfil de `applyToJob` não servem ao caminho institucional). E-mail no Outbox `topic='email'` template `referral-notification` na mesma tx (no-op quando Pessoa sem e-mail; dispatcher real = USP-044, AD-007). Eventos `REFERRAL_CREATED`/`REFERRAL_RESULT_REGISTERED` já existiam. Must-nots REF-MN-01..04 (037) + REF38-MN-01..03 (038). Fix de build: `ApplyConflictError` movido de `apply-to-job.ts` (`'use server'` só exporta funções async) p/ `jobs/domain/apply-errors.ts` (relocação pura). 8+3 tasks.
- **U3 (USP-039)** — View Model `viewPersonForSocialAssistant` em `persons`, rota `(app)/pessoas/[id]/visao-consolidada` (sub-rota dedicada, não sobrecarrega a página de gestão USP-007). **Padrão composition-root** p/ evitar o ciclo de import AD-019: `persons` **não** importa os barrels de jobs/referrals/services/companies em runtime (só `import type`, apagado por isolatedModules) — a **página** busca as 5 dimensões externas e passa ao assembler. Reusa `getSocioeconomicRecord`/`viewSocioeconomicRecord` (ficha), `viewPersonForStaff` (identidade/papéis), `listProviderServices`; **4 reads novos** mínimos por dimensão (`listPersonApplications` jobs, `listPersonReferrals` referrals, `listPersonServiceInterests` services, `listPersonCompanyGrants` companies — `select` restrito sem PII de terceiros, `take`, int-test contra o `where` real por AD-021). **Omissão da ficha ao coordenador em 2 barreiras:** B1 — o assembler só chama `getSocioeconomicRecord` quando `canManageSocioeconomicRecord(roles)`; p/ coordenador nunca é chamado → colunas sensíveis nunca SELECTadas nem no payload Flight. B2 — `ConsolidatedPersonView.ficha` só populado no ramo AS/BOARD (strip estrutural). Voluntário comum negado na rota (`notFound()`) **e** no assembler (defesa em profundidade). AS/BOARD emite `SENSITIVE_FIELD_VIEWED` via o read da ficha (sem evento novo). Must-nots SOC-039-MN-01 (sem ficha p/ coordenador/não-AS-BOARD) / MN-02 (voluntário zero-dados). 8 tasks. **Deferido (flag, não-bloqueante):** area-scoping row-level do coordenador (quais Pessoas ele pode abrir) **não** é modelado no MVP (não há vínculo Pessoa↔área) — coordenador abre o painel operacional de qualquer Pessoa (mesma superfície da página USP-007), mas o sensível fica protegido por MN-01.
**Reason:** A Fase 5 fecha a frente social/institucional: AS registra a ficha socioeconômica, encaminha Pessoa a vaga em nome da ASONSEG (ativando papel candidato + candidatura com badge + e-mail), acompanha o resultado, e enxerga tudo num painel consolidado com visibilidade por papel. Habilita as métricas MP8 (encaminhamentos criados) e MP9 (% com resultado positivo). O dono pediu "toda a Fase 5 em 1 PR".
**Trade-off:** PR grande (revisão pesada) — aceito explicitamente pelo dono. 4 flags de review levantadas (todas resolvidas por precedente/spec, não bloqueiam dev): (1) cripto-em-repouso = plataforma vs. coluna; (2) ficha sem `requireActiveConsent` (legítimo interesse); (3) encaminhamento sem gate `PORTAL_ACCESS` (Pessoa sem credencial, mandado pela edge case); (4) area-scoping do coordenador não modelado (follow-up DPO/diretoria). E2E autenticado limitado aos gates de sessão/RBAC (padrão L-007); cobertura autoritativa do write-path/privacidade nos testes de integração/componente.
**Impact:** 3 Verifiers independentes PASS, 0 fix→re-verify (todos os units passaram de primeira). Sensores matando as mutações-alvo: vazamento de campo sensível da ficha ao viewer não-AS/BOARD (RSC/Flight), write sem auditoria, unicidade da candidatura ativa sob concorrência exercitando o índice real do DB (L-010), revalidação vaga-ATIVA/TOCTOU, resumo-quando-sem-CV, enum de resultado (Zod + PG-enum), autor+data do resultado, negação do voluntário, omissão da ficha ao coordenador em 2 barreiras. Gates no HEAD `97f772d`: typecheck/lint verdes, 1354 unit, 561 integração, build OK, 2 migrações limpas. Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/ficha-social-encaminhamento/usp-03{6,7,8,9}-*/`. Lições candidatas L-011 (checar família de rota existente antes de propor prefixo novo) e L-012 (tipar o parâmetro de papel do guard como `string[]` da sessão, não `Role[]` do Prisma). Desbloqueia a Fase 6 (relatórios/home/hardening).

### AD-021: Fase 4 — Remediação da review multi-agente do PR #284 — PASS 2026-07-09

**Decision:** Corrigir **todos os 7 achados** da review multi-agente do PR #284 na **mesma branch** `feat/fase-4-servicos-manifestacoes` (PR aberto, não mergeado), pelo pipeline Planner→Implementer→Verifier, execução autônoma. Não é linha-USP do ROADMAP (Fase 4 já `[x]` via AD-020) — ROADMAP não foi tocado. Artefatos em `.specs/features/fase-4-review-fixes/`. Achados e correções:
- **F1 🚨** CTA "Publicar primeiro serviço" (USP-010, `provider-form.tsx`) apontava para `/servicos/novo` (rota inexistente → caía no detalhe público `id="novo"` → `ServicoIndisponivel`); corrigido para `/prestador/servicos/nova` (rota real da USP-029), teste-âncora que fixava o href errado corrigido junto.
- **F2 🔒** `uploadServicePhoto` só exigia sessão → qualquer Pessoa autenticada gravava no bucket público `provider-photos`. Adicionado **gate de papel PROVIDER** antes do Storage (decisão: **só papel, não** consent `SERVICE_OFFERING` — o passo de upload pré-serviço ainda não exige a finalidade e adicionar quebraria os happy-paths verdes; teto de volume = follow-up documentado). Materializado como must-not **MN-F2** com teste negativo.
- **F3 🔒** `photoStoragePaths` vinha do cliente sem validação de posse/formato → possível misatribuir foto de outro prestador ou injetar `../` interpolado na URL pública. Novo helper puro `services/domain/photo-path.ts` (regex estrito `${uuid}/${uuid}.(jpg|png|webp)` + ownership `startsWith(person.id + '/')`) cabeado em `createServiceDraft` **e** `submitServiceForModeration` antes do `create`, rejeita com `VALIDATION`. Must-not **MN-F3** com teste unit + integração ("nenhuma linha persistida").
- **F4 ⚡** busca por nome de categoria usava `OR sc.name` sobre a relação juntada → derrubava o índice GIN trgm `service_search_trgm` (seq scan das ACTIVE). Reescrito como `services.category_id IN (subselect)` (ambos ramos viram predicado de `services`, index-elegível via BitmapOr); `LEFT JOIN service_categories sc` morto removido de page+count. **Query-only, sem migração** (índice já estava correto).
- **F5/F6/F7 ⚠️** três queries de leitura (`listServiceCategories` `isSuggestion:false`; `listProviderServices` escopo `authorPersonId`; `getMyActiveServiceInterest` `cancelledAt:null`) tinham o `where` só mockado em page tests → adicionados testes de integração exercitando o filtro real.

**Reason:** Fechar a review do PR #284 antes do merge. Os 2 🔒 e o 🚨 têm impacto real (upload aberto a não-prestadores, misatribuição/traversal de path, CTA morta que impedia o prestador de chegar ao formulário que a fase inteira habilita); os ⚠️ eram lacunas de cobertura.
**Impact:** Verifier independente **PASS** com teste de mutação ao vivo — 3 mutações injetadas (gate PROVIDER desligado; `&&`→`||` no ownership; `IN`→`NOT IN` no subselect), 3 mortas, 0 sobreviventes (MN-F2/MN-F3 são sensores reais). Gates no HEAD `1e6053f`: typecheck/lint verdes, **1252 unit, 495 integração**, `NODE_ENV=production` build OK (rota `/prestador/servicos/nova` compila), sem migração nova, sem deleção não-relacionada commitada. 10 commits `f15337c..1e6053f` (fix/perf/test/docs por achado). Commits **locais** — push do branch e resposta/resolução dos comentários inline pendentes de decisão do dono.

### AD-020: Fase 4 — Serviços + Manifestações, rodada única em 1 PR — PASS 2026-07-08

**Decision:** Executar a **Fase 4 inteira** (9 USPs) em **um único PR**, a pedido do dono, planejada→implementada→validada em 3 unidades pelo pipeline Planner→Implementer→Verifier (mesmo modelo de AD-015/016/019), execução autônoma. Cada USP planejada "como se nova"; as já implementadas foram **refatoradas/reconciliadas**; as novas foram **implementadas** de fato. **Unidades:** **U1** cadastros de papel (USP-010 prestador refatorada ao DS + USP-011 cliente reconciliada) · **U2** serviços (módulo novo `services`: USP-029/030/031/032) · **U3** manifestações (`ServiceInterest`: USP-033/034/035). Decisões técnicas:
- **U1** — USP-010 restyle style-only ao DS (AD-014), guard `PRV-MN-01`, backend intocado; USP-011 **sem UI self-service** — o papel CLIENT é o mais leve e **nasce na 1ª manifestação de interesse** (auto-ativado dentro de `services.manifestInterest` via `ensureClientRole`), coerente com o comentário do model `ClientProfile` e o intent de fricção zero. Deliverable da USP-011 = helper `ensureClientRole(tx, {personId, term, ip, userAgent})` + `ClientProfile` leve (sem `publicationStatus`) — já implementado, verificado sem gap.
- **U2** — `Service` segue o padrão status-na-entidade (AD-009), NÃO `content_items`; `ContentKind.SERVICE`/`TRANSITIONS[SERVICE]` já existiam, faltava `PrismaServiceStatusRepository` (registrado no `container.ts`), estender `eventTypeFor` p/ SERVICE (pausar/despausar/arquivar — era pré-req crítico), 3ª fonte na fila de moderação e 3 eventos de auditoria novos (`SERVICE_DRAFT_SAVED`/`SERVICE_UNPAUSED`/`SERVICE_EDITED_AFTER_APPROVAL`). Publicação = moderação **por serviço** (não por prestador); gate = papel PROVIDER ativo + `requireActiveConsent('SERVICE_OFFERING')` (+ responsável ativo se `companyId`); serviços **não** dependem de `company.isVerified` (hook de verificação é jobs-only, no-op p/ SERVICE). Busca AD-011 (unaccent/trgm, on-read `ACTIVE + prestador ativo`, paginada, ISR). Fronteira de privacidade **diferente de vagas**: o **nome** do prestador/empresa é público (ADR-0010); só o **contato (phone/emailLogin) é oculto de todos** em U2 — `viewServiceDetail` é fonte única (página + `generateMetadata` + JSON-LD) e nunca faz SELECT do contato (defesa RSC/Flight em 3 camadas: select + View Model explícito + tipo sem o campo). USP-032 `editService` = exceção atômica documentada de escrita conteúdo+status (guard `SVC032-MN-01`). Storage: bucket `provider-photos` 5MiB + `image/webp`. Migrações `usp029_service` (+dedup parcial por autor) e `usp030_service_search` (trgm). U2 deixou o **seam do CTA** sem persistência e o `Service` **sem** relação `interests` (adicionada incrementalmente na U3, mesmo padrão AD-012→USP-025).
- **U3** — `ServiceInterest` vive em `@/modules/services` (pai = Service, precedente AD-017 de Application em `jobs`); migração `usp033_service_interest` adiciona a tabela + relações `Service.interests`/`Person.serviceInterests` + **índice único parcial** `uq_service_interest_active ... WHERE cancelled_at IS NULL` (1 interesse ativo à prova de concorrência, re-manifestar após cancelar). `manifestInterest` **auto-ativa o papel CLIENT na mesma tx auditada** via `ensureClientRole` + consent `SERVICE_HIRING` (cujo revoke cascateia o grant CLIENT→REVOKED, logo consent-inativo ⟺ CLIENT-não-ATIVO). **Contato = a Person autora** (Company não tem campos de contato); revelado ao cliente **enquanto ele tem interesse ativo** (some da UI após cancelar). Autorização self-service sem RBAC (sessão + pré-condição + consent). USP-034 cancel owner-scoped/idempotente (diverge de `cancelApplication`: re-cancel é idempotente OK, AC-034-3). USP-035 owner-scoped por `service.authorPersonId`, `viewClientForProvider` (espelha `viewCandidateForEmployer`), audit-on-read via `SENSITIVE_FIELD_VIEWED`. E-mail ao prestador enfileirado no Outbox `service-interest-notification` (dispatcher = USP-044). Eventos `INTEREST_MANIFESTED`/`INTEREST_CANCELLED`/`PROVIDER_CONTACT_REVEALED` já existiam no catálogo.
**Reason:** A Fase 4 tinha USP-010 entregue sem DS e USP-011 parcialmente scaffolded; serviços e manifestações inexistentes. O dono pediu "planejar como novo + refatorar as feitas + implementar as novas, tudo em 1 PR". Fecha a frente de serviços fim-a-fim (prestador publica serviço moderado, cliente busca/manifesta interesse e recebe contato).
**Trade-off:** PR grande (revisão pesada) — aceito explicitamente pelo dono. Refactor de U1 é style-only. E2E autenticado deferido (padrão do repo, L-007); cobertura autoritativa do write path/privacidade nos testes de integração/componente, E2E cobre o gate de sessão/PROVIDER (specs reais). Divergências declaradas do TD §2.6 (Service: `authorPersonId`, `priceUnit`, single `regionId`; ServiceInterest: índice parcial supersede compound-unique) documentadas nos design.md. Multi-região e filtro de disponibilidade estruturado deferidos.
**Impact:** 3+1 Verifiers independentes PASS (U3 teve 1 fix→re-verify: sensor `SVC033-MN-03` mascarado por pré-check da app → fortalecido p/ exercitar o índice do DB direto, discriminação provada 8/8 red com índice dropado / 8/8 green restaurado; achado ortogonal de deadlock `40P01` em `ensureClientRole` sob concorrência corrigido test-only). Sensores matando as mutações-alvo (vazamento de contato do prestador a viewer não-habilitado, on-read ACTIVE-only, gate de ownership de pause/edit/lista, `CONSENT_REQUIRED` vs `VALIDATION`, unicidade sob concorrência via índice parcial). Gates no HEAD: typecheck/lint verdes, 1237 unit, ~486 integração, build OK, 3 migrações limpas. Artefatos por-USP em `.specs/features/{cadastros-publicos,servicos,manifestacao-interesse-servico}/usp-*/`. Lição candidata L-010 (sensor de concorrência mascarado por defesa da app — exercitar a garantia real, não o outcome que ambas as camadas satisfazem). Desbloqueia Fase 5 (USP-036 ficha socioeconômica → encaminhamento).

### AD-019: Fase 3 — Candidaturas + Busca + Extração de CV, rodada única em 1 PR — PASS 2026-07-08

**Decision:** Executar a **Fase 3 inteira** (6 USPs) em **um único PR**, a pedido do dono, planejada→implementada→validada em 4 unidades pelo pipeline Planner→Implementer→Verifier (mesmo modelo de AD-015/AD-016), execução autônoma. Cada USP planejada "como se nova"; a já implementada (**USP-009**) foi **refatorada** ao Design System (AD-014, style-only, comportamento ancorado nos testes verdes como testes negativos); as 5 sem código foram **implementadas** de fato. **Unidades:** **U1** USP-009-refactor (2 telas → `@/shared/ui`, guard anti-deriva `CAD-MN-03`) · **U2** candidaturas (025/026, agregado `Application` write path — AD-017) · **U3** busca de candidatos (027/028 — View Models `viewCandidateForEmployer`/`viewCandidateForSearch`, AD-018) · **U4** extração de CV via IA (040). Decisões técnicas das unidades vivem em AD-017 (U2) e AD-018 (U3). **U3 fechou um débito de fase:** o schema de candidato (USP-009) coletava/validava `phone` mas `activateCandidateRole` **nunca o persistia** — corrigido (`fix(persons)`: grava `Person.phone` na mesma tx auditada), pois USP-027 mostra o contato. **U4 (cv-extraction, módulo novo):** porta `CVExtractor.extract → {ok:true,fields,usage}|{ok:false,reason}` (nunca lança → fallback gracioso estrutural); único arquivo que importa `@anthropic-ai/sdk` é o `AnthropicCVExtractor` (guard estático `CVE-MN-05`); `FakeCVExtractor` sob flag `CV_EXTRACTOR_FAKE` guardada por `VERCEL_ENV`; 3 Server Actions separadas (upload/extract/confirm) para materializar os must-nots em fronteiras testáveis — **a IA nunca grava** (só `confirmCvFields` persiste; extract é read-only); validação de **MIME real** (magic bytes, não extensão) + ≤5MB; consentimento `CV_AI_EXTRACTION` reverificado entre upload e extract (revogação interrompe sem chamar o LLM); rate limit **durável** via tabela nova `CvUploadAttempt` (espelha `AuthAttempt`, 3/dia por candidato, TZ `America/Sao_Paulo`); custo/tokens/duração só como metadado do `CV_EXTRACTION_COMPLETED` (nunca o conteúdo do CV); evento `CV_UPLOADED` adicionado ao catálogo; deps novas `@anthropic-ai/sdk` + `mammoth` (DOCX→texto; PDF nativo). Bug de import circular (barrel `@/modules/persons` arrasta Prisma p/ o bundle client) encontrado e corrigido duplicando `EDUCATION_LEVELS` local (mesmo padrão do carve-out client/server, sem expandir o allowlist do `no-deep-module-imports`).
**Reason:** A Fase 3 tinha 1 USP entregue sem DS e 5 pendentes (candidaturas eram só contador — AD-012; `cv-extraction`/`referrals` inexistentes). O dono pediu "planejar como novo + refatorar as feitas + implementar as novas, tudo em 1 PR". Isso destrava a empregabilidade fim-a-fim (candidato se candidata, empresa vê/busca candidatos, CV pré-preenchido por IA) e uniformiza a tela de candidato no DS.
**Trade-off:** PR grande (revisão pesada) — aceito explicitamente pelo dono. Refactor de U1 é style-only behavior-preserving; as novas exercitam a matriz de Server Action (happy/Zod/authz-precondição/consent/idempotência/concorrência). **E2E autenticado deferido** em todas as USPs novas — este repo não tem infra de seed de sessão Supabase no Playwright (padrão documentado em 5 specs pré-existentes); a cobertura autoritativa do write path/privacidade vive nos testes de integração/componente, e os E2E cobrem o gate de sessão (specs reais, não `.fixme`, L-007). Coleta de **região** do candidato (form) fica como follow-up (busca da USP-028 é estruturalmente correta mas inerte p/ dados reais até isso; seed cobre E2E). Divergências declaradas de TD §4.5 documentadas em AD-017.
**Impact:** 4+ Verifiers independentes PASS (U2 re-rodado após morte por erro de API do 1º verifier, tree restaurada); sensores matando as mutações-alvo (unicidade da candidatura ativa sob concorrência, revogação de consent → sem LLM, no-persist-sem-confirm, guard do SDK, vazamento de CPF/sobrenome no payload RSC/Flight, gate de ownership da lista de candidatos, persistência do `Person.phone`). Gates no HEAD: typecheck/lint verdes, 1121 unit, 385 integração, `NODE_ENV=production` build OK, 3 migrações aplicam limpas. Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/{cadastros-publicos,candidaturas-busca-candidatos,extracao-cv-ia}/usp-*/`. Lição candidata L-009 (AC de latência p95 sem asserção automática de tempo). Desbloqueia Fase 4 (USP-011 cadastro de cliente → serviços/manifestações) e Fase 5 (USP-037 encaminhamento liga `Application.viaEncaminhamento`).

### AD-018: USP-028 (Fase 3 U3) — `CandidateProfile.regionId` (localização estruturada do candidato)

**Decision:** USP-028 (Empresa buscar candidatos — busca ativa) introduz `CandidateProfile.regionId String? @map("region_id") @db.Uuid` + relação `Region?` (espelha `ProviderProfile.regionId`, mesma FK `ON DELETE SET NULL ON UPDATE CASCADE`), migração `20260708150000_usp028_candidate_search`. A coluna nasce **nullable**: a coleta de região no formulário de cadastro de candidato (USP-009, já entregue) é **follow-up explicitamente fora do escopo** de U3 — candidatos reais ficam sem `regionId` até essa correção; a busca/exibição por localização tratam `null` com graça ("Região não informada", filtro simplesmente não casa). A mesma migração cria o índice funcional `candidate_search_trgm` (GIN/trgm sobre `headline+skills_text+courses_text+experience_text`, reusando `immutable_unaccent`/`pg_trgm` já habilitados pela USP-021).
**Reason:** Precedente do projeto (AD-011/012): a 1ª USP que precisa de um campo estruturado cria a migração mínima; `Region` (taxonomia) já existe e é reusada, não reinventada. Reabrir o form da USP-009 (já entregue) para coletar região é escopo maior e não bloqueia a busca (que opera estruturalmente correta, só inerte para dados sem região).
**Trade-off:** Busca por localização e exibição de cidade/região ficam **inertes** para candidatos reais até o follow-up popular `regionId` (form ou backfill); seed demo popula a coluna para o E2E ter dados.
**Impact:** Desbloqueia `searchCandidates`/`viewCandidateForSearch` (USP-028). Artefatos em `.specs/features/candidaturas-busca-candidatos/usp-028-empresa-buscar-candidatos/{spec,design,tasks}.md`.

### AD-017: USP-025/026 (Fase 3 U2) — caminho de escrita do agregado `Application` (kickoff 2026-07-08)

**Decision:** A unidade U2 da Fase 3 (USP-025 candidatar-se + USP-026 cancelar) entrega o **caminho de escrita** que a AD-012 deferiu, planejada coesa como **um agregado**: **1 migração** e **1 módulo** de Server Actions. Decisões de projeto (a que USP-027/028/037/044 conformam): **(1) Módulo dono = `jobs`** — não há módulo `candidaturas` na lista canônica de 11 (`src/` fechado, novo módulo = RFC); a relação `Application` mora em `Job`, AD-012 pôs a leitura (contador) em `jobs`, TD §2.5 agrupa "Vagas e candidaturas"; `referrals` (Fase 5) importará `applyToJob` via barrel `@/modules/jobs`. **(2) Unicidade por índice ÚNICO PARCIAL** `CREATE UNIQUE INDEX uq_application_active ON applications (candidate_person_id, job_id) WHERE cancelled_at IS NULL` (SQL bruto na migração — Prisma não expressa `@@unique ... WHERE`; padrão `uq_person_company_active`/`consents_active_purpose_unique`). Garante 1 candidatura ativa à prova de concorrência (P2002→`CONFLICT`) **e** habilita recandidatura após cancelar (a linha cancelada sai do índice). **Supersede** o `@@unique([jobId, candidatePersonId, appliedAt])` do TD §4.5 (que não impede duplicata ativa — appliedAt difere): NÃO adotado. **(3) `viaEncaminhamento Boolean @default(false)` materializado agora** (sempre `false` na Fase 3) em vez da FK `viaReferralId` do TD §4.5 — o model `Referral` é USP-037/Fase 5 e não existe; a USP-037 adiciona a FK e passa a setar o boolean, sem re-migrar. **(4) Autorização self-service SEM RBAC** — não existe `PermissionId` de candidatura; o passo `requirePermission` da sequência canônica é substituído por sessão (`getCurrentPerson`) + pré-condição `CandidateProfile.publicationStatus=ACTIVE` + `requireActiveConsent(JOB_APPLICATION)` (padrão `activate-candidate-role.ts`). E-mail de confirmação **enfileirado** no `Outbox` (`topic='email'`, novo template `application-confirmation`) na mesma tx do `withAudit(APPLICATION_CREATED)` — dispatcher real = USP-044 (AD-007). Cancelamento: escopo por dono (`findFirst` a `candidatePersonId=person.id` → `NOT_FOUND` sem vazar existência) + idempotência defense-in-depth (pré-check + optimistic `updateMany where cancelledAt:null`).
**Reason:** AD-012 explicitou que a escrita/unicidade/encaminhamento seriam da USP-025/044. O contrato do TD §4.5 divergiu do que é implementável na Fase 3 (Referral inexistente) e do que garante a invariante de negócio (compound-unique não previne duplicata ativa). Fonte da verdade do schema real (AD-012) > redação do TD.
**Trade-off:** Divergência declarada de TD §4.5 em 2 pontos (viaReferralId→viaEncaminhamento boolean; compound-unique→índice parcial), documentada nos design.md das duas USPs. UI é fatia vertical fina na página **pública** de detalhe (`(public)/vagas/[id]`), que já é dinâmica por `getCurrentPerson()`; CTA autenticado é aditivo (anônimo continua sem CTA — P-003/P-005 da USP-022 preservado).
**Impact:** Desbloqueia USP-027 (badge de encaminhamento lê `viaEncaminhamento`; contato revelado após candidatura) e USP-037 (adiciona `viaReferralId`). Artefatos por-USP em `.specs/features/candidaturas-busca-candidatos/{usp-025-candidatar-se,usp-026-cancelar-candidatura}/{spec,design,tasks}.md`. **Status:** planejamento concluído; aguarda Execute (Implementer→Verifier). Lição L-007 aplicada (E2E promovido ao diretório real `e2e/`).

### AD-016: Fase 2 — Empresas + Vagas + Moderação, rodada única em 1 PR — PASS 2026-07-08

**Decision:** Executar a **Fase 2 inteira** (13 USPs) em **um único PR**, a pedido do dono, planejada→implementada→validada em 5 unidades pelo pipeline Planner→Implementer→Verifier (mesmo modelo do AD-015), execução autônoma. Cada USP planejada "como se nova"; as já implementadas foram **refatoradas** ao Design System (AD-014) + consistência (style-only, comportamento preservado ancorado nos testes verdes como testes negativos); as 4 sem código foram **implementadas** de fato. **Unidades:** U1 Empresas (USP-012/013/014/015, refactor) · U2 Moderação-refactor (016/017) · **U3 Moderação-novas (018 inativar conteúdo, 019 sugerir categoria)** · U4 Vagas-refactor (020/021/022) · **U5 Vagas-novas (023 editar/pausar/arquivar/renovar, 024 expiração automática)**. Decisões técnicas das novas: **USP-018** inativa via `transitionContent` (→`INACTIVATED` terminal, exclusão on-read nas queries públicas). **USP-019** sem entidade/migração nova — estado derivado de `JobArea`/`ServiceCategory` (`isSuggestion`/`approvedAt`), aprovação por `requirePermission('APPROVE_CATEGORY_SUGGESTION')`, dedup `foldForDedup`, novo evento `CATEGORY_SUGGESTION_REJECTED`; entrada de sugestão via escape-hatch de import (client não pode puxar o barrel de moderation). **USP-023** pausar/arquivar/renovar via FSM (`eventTypeFor` tornado kind-aware p/ JOB), editar vaga ACTIVE força re-moderação (→DRAFT), estender validade NÃO transiciona; `editJob` é a **exceção documentada** de escrita atômica conteúdo+status fora do `transitionContent` (guardada por teste estático). **USP-024** expiração por **Vercel Cron** (`/api/cron/expire-jobs`, clone do `auth-attempts-retention`: `CRON_SECRET` constante, fail-closed) materializando `EXPIRED` via `transitionContent(SYSTEM_JOB)`, idempotente (`updateMany where status=ACTIVE`), TZ `America/Sao_Paulo`; 1 migração `usp024_expiry_reminder` (`Job.expiryReminderSentAt`), seed de `SYSTEM_ACTOR_ID`. `publishedAt` passa a ser escrito no adapter (coluna já existia, sem migração).
**Reason:** Fase 2 tinha 9 USPs entregues sem os artefatos por-USP e sem o DS, e 4 USPs pendentes. O dono pediu "planejar como novo + refatorar as feitas + implementar as novas, tudo em 1 PR". Isso fecha a fase e uniformiza as telas de Empresas/Vagas/Moderação nas primitivas de `@/shared/ui`.
**Trade-off:** PR grande (revisão pesada) — aceito explicitamente pelo dono. Refactors são style-only + consistência behavior-preserving; only-backend novos exercitam a matriz de Server Action (happy/Zod/authz/precondição/idempotência). U5 exigiu 1 ciclo fix→re-verify (as specs E2E ficaram como `.fixme` e os guards de página sem teste — corrigido: specs reais em `e2e/jobs/` + `page.test.tsx`). Corrigido de passagem o flake de fuso `dateOffset()` (L-006, teste-only) e o `ci.yml` (faltava seed demo antes do Playwright → E2E de vagas quebraria em CID; seed inserido **após** integração p/ não colidir com o cleanup, cf. memória seed-cnpj-exclusivo).
**Impact:** 5+1 Verifiers independentes PASS; sensores matando as mutações-alvo (ownership de `pauseJob`, idempotência de `runJobExpiration`, `eventTypeFor` errado, drop da re-moderação, guards de página, dedup, auth anônima). Gates no HEAD: typecheck/lint verdes, 995 unit, 325 integração, build OK, migração limpa. Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/{cadastros-publicos,vinculos-pessoa-empresa,moderacao-conteudo,vagas}/usp-*/`. Lições L-005..L-008 candidatas. Desbloqueia Fase 3 (USP-025 candidatar-se depende de USP-020, agora com ciclo de vida completo).

### AD-015: Fase 1 — refactor amplo (design system + consistência), rodada ad-hoc — PASS 2026-07-07

**Decision:** Rodada ad-hoc de refactor da Fase 1 (Identidade + Consentimentos), a pedido do dono, planejada→implementada→validada em 6 unidades pelo pipeline Planner→Implementer→Verifier, cada unidade com Verifier independente PASS. Não é linha-USP do ROADMAP (Fase 1 já estava 100% `[x]`); ROADMAP não foi tocado. Branch `refactor/fase-1-design-system-e-consistencia`, 1 PR único. Decisões do dono (locked): (a) extrair o design system do protótipo e aplicar a TODAS as telas da Fase 1; (b) preservar os fluxos LGPD/arquitetura, aplicando só o estilo visual (sem fundir cadastro+consentimento+perfil); (c) refactor amplo, execução autônoma. **Unidade 0** = Fundação Design System (AD-014). **Grupo A** USP-001+USP-043: restyle + guarda de sessão/token defense-in-depth no `acceptRoleConsent` (TX2 não tem sessão → re-valida o token HMAC assinado dentro da action). **Grupo B** USP-004+USP-005: restyle (login já feito na Un.0) + padroniza `changePassword` p/ `getCurrentPerson()` (ADR-0030). **Grupo C** USP-002+USP-003: restyle; **decisões de consistência documentadas sem mudança de código** — USP-002 fica em `identity` (mover p/ `persons` arrisca o carve-out client/server do ADR-0017), USP-003 mantém authz inline do aprovador (não há `PermissionId` de catálogo p/ aprovar reivindicação). **Grupo D** USP-006+USP-008: restyle + extração dos schemas Zod inline do USP-008 p/ `schemas/delegated-permission.schema.ts` (byte-a-byte, `requireCoordinator` já canônico). **Grupo E** USP-007+USP-045: restyle + **backfill da spec da USP-045** (extra do board/IDSD, sem base no PRD) a partir do código + como inverso da USP-007; variante `danger` aditiva no `Button` do DS.
**Reason:** Fase 1 foi implementada sem os artefatos de planejamento por-USP (só specs de épico, AD-001) e sem adotar o design system do protótipo (`src/shared/ui` vazio). O dono pediu para "planejar como funcionalidade nova + refatorar + paridade visual". Descoberta central: o protótipo é um site público de marketplace e **não contém ~10 das ~12 telas da Fase 1** — resolvido extraindo o design system (AD-014) e aplicando sua linguagem visual a todas as telas, e preservando os fluxos LGPD onde o protótipo conflitava (aceite afirmativo versionado > checkbox inline; perfil de candidato fica na USP-009).
**Trade-off:** Todo restyle é style-only (markup/classes), com o comportamento preservado ancorado em testes existentes verdes como testes negativos. As únicas mudanças de backend foram consistência pura e behavior-preserving: guarda do `acceptRoleConsent`, `changePassword`→`getCurrentPerson`, extração dos schemas do USP-008. Correção de recon: o port `CompanyResponsibility` já usa o adapter Prisma real (não o stub null) — precondição P-002 da USP-007 é genuinamente aplicada. D-005 (catálogo de motivos de reativação) segue como item aberto não-bloqueante da USP-045.
**Impact:** 6 Verifiers independentes PASS; sensores de discriminação matando as mutações-alvo (incl. o zeramento de grants R2 da USP-045 e a guarda do `acceptRoleConsent`). Gates no HEAD: typecheck/lint verdes, 859 unit, 219 integração, build OK. Artefatos por-USP (spec/design/tasks/validation) em `.specs/features/{identity-acesso-papeis,consentimentos-lgpd}/usp-*/` e `.specs/features/fundacao-ui-design-system/`. Lições L-001..L-004 em `.specs/lessons.json`/`LESSONS.md` (candidatas). As telas da Fase 1 agora compartilham as primitivas de `@/shared/ui` — base p/ reestilizar as fases seguintes.

### AD-014: Convenção de Design System da Fase 1 — extração do protótipo p/ `src/shared/ui` (PASS 2026-07-07)

**Decision:** Fundação de design system extraída do protótipo `docs/prototipo/index.html` (Unidade 0 do refactor da Fase 1), planejada→implementada→validada pelo pipeline Planner→Implementer→Verifier. Convenções fixadas: **(1) tokens** em `src/app/globals.css` (variáveis CSS light/dark) + `tailwind.config.ts` (chaves semânticas `primary`/`cta`/`surface`/`fg`/`border`/…, radius/shadow/font); paridade verbatim com o protótipo (cores, ex. `--color-primary #2563EB`, `--color-cta #F97316`). **(2) Primitivas** em `src/shared/ui/` (barrel `index.ts`): `cn`, `Button` (cva+Radix Slot), `Input`/`Label`/`Textarea`, `Card`/`FormCard`/`FormSectionTitle`, `FormHeader`/`StepIcon`, `FormRow`, `LgpdBox`/`LgpdCheck`, `Badge`, `ThemeToggle`/`ThemeScript`. **(3) Dark mode** via `data-theme="dark"` no `<html>` com cascata de variáveis CSS (sem `dark:` na maioria); `ThemeScript` anti-FOUC + `ThemeToggle` só com React state — **sem `next-themes`/lib de estado**. **(4) Fontes** auto-hospedadas via `next/font/google` (Nunito heading + DM Sans body) — **sem CDN externo** (prod sem host externo). **(5) Deps** allowlist: `class-variance-authority`, `clsx`, `tailwind-merge`, `@radix-ui/react-slot`, `@radix-ui/react-label`. Prova de paridade: login reestilizado preservando RHF/Zod/`loginAction`/anti-enumeração.
**Reason:** O protótipo carrega um design system completo que o código nunca adotou (`src/shared/ui` estava vazio; `globals.css` só tinha tokens genéricos). O dono decidiu extrair e aplicar a TODAS as telas da Fase 1 (mesmo as ~10 sem contrapartida no protótipo), para consistência visual. Fundação primeiro destrava o restyle das demais unidades sem retrabalho.
**Trade-off:** 5 must-nots (DS-MN-01..05) materializados com guarda negativa discriminante cada (fontes externas, hex cru em `shared/ui`, mecanismo duplo de dark, dep proibida, paridade do login). Tints de Badge/StepIcon usam `color-mix()` sobre tokens em vez do hex literal do protótipo (deriva ~3/canal no light) p/ respeitar DS-MN-02 — aprovado. Nenhuma tela real da Fase 1 (fora o login) foi reestilizada nesta unidade — é foundation-only.
**Impact:** Verifier independente PASS — typecheck/lint verdes, 848/848 unit, build OK, 7/7 mutações mortas. Artefatos em `.specs/features/fundacao-ui-design-system/{spec,design,tasks,validation}.md`. Todas as unidades seguintes (Grupos A–E) reestilizam as telas consumindo estas primitivas.

### AD-013: Fase 0 — Fundação reconciliada aos docs canônicos (ad-hoc, fora do ROADMAP) — PASS 2026-07-06

**Decision:** Rodada ad-hoc de Fase 0 (não é linha-USP do ROADMAP; ROADMAP não foi tocado) cobrindo 3 frentes, planejada→implementada→validada pelo pipeline Planner→Implementer→Verifier. **WS-A (scaffolding):** guardas estáticas de arquitetura (`no-deep-module-imports`, `closed-src-root`, `no-committed-secrets` — padrão `no-external-verify.test.ts`), split de `prisma/seed.ts` em `seeds/reference.ts` (idempotente, prod-safe) + `seeds/demo.ts` (dev-only), client `supabase-storage.ts` (ADR-0005), e nota de conformidade `docs/arch/0017-conformidade-fundacao-fase-0.md`. **WS-B (seed-taxonomia-checklists, US-111):** seed de taxonomia alinhado à lista canônica de `docs/operacao/taxonomia-inicial.md` com teste de integração idempotente (AC-111-1), e itens da checklist de verificação de Empresa migrados de const TS para **modelo Prisma seedável** `VerificationChecklistItem` + query `listVerificationChecklistItems()` no barrel `@/modules/moderation` + rewire do `VerificationPanel` com fallback (F0B-01 — **completa AD-010 e realiza o mecanismo de B-004: troca de itens sem redeploy**). **WS-C (runbook):** `docs/infra/fase-0-provisioning-runbook.md` reconciliando cada serviço externo (estado atual/provisionar/verificar) + guarda anti-segredo defensiva.
**Reason:** Projeto já avançado (Fases 1–2) pedia reconciliar a fundação aos guidelines antes de seguir. 5 must-nots (F0-MN-01..05) materializados com teste negativo discriminante cada. O "achado de segredo vazado" do plano original era **falso-positivo** — `.env.staging` já era untracked + gitignored (0 commits); T-C2 virou guarda defensiva com allowlist do JWT demo público do Supabase e fakes de CI.
**Trade-off:** T-A1 — os 3 deep-imports de `persons/components/*form.tsx` **não** foram roteados pelo barrel: é carve-out client/server real (o barrel `@/modules/identity` puxa `next/headers`/`next/cache` transitivamente → quebra o build num Client Component), documentado no ADR-0017; a guarda os permite via escape-hatch com justificativa, com o conjunto de exceção pinado a exatamente 2 arquivos. T-B3 dividido em B3a/B3b. Migração `20260706231944_...` editada à mão para conter só a nova tabela, sem arrastar drift pré-existente do `jobs_area_id_fkey` (RESTRICT preservado). Módulos canônicos ausentes seguem deferidos às USPs donas (AD-005/AD-009).
**Impact:** Verifier independente PASS — build/lint/typecheck verdes, unit 751/751, integração 217/217, sensor 6/6 mutações mortas. Artefatos em `.specs/features/fase-0-fundacao/{spec,design,tasks,validation}.md`. Gates de conteúdo (B-004 checklist assinada, taxonomia final) seguem como pendências de go-live, não de desenvolvimento.

### AD-012: USP-022 — introduzir a tabela `applications` (só contagem) + materializar must-nots sub-especificados (kickoff 2026-06-20)

**Decision:** USP-022 (#172) entrega o detalhe da vaga. Gate de entrada ICE **passou** (sem Q-aberta/ADR Proposed/premissa aberta; upstream USP-021 em master). Classificada **Large** (ICED + must-nots P-001..P-005). A tabela `applications` do TD §4.5 **não existe** — `#162` (schema USP-020) criou só `Job`/`JobArea`/`Region`/`ContentStatus`; `JOB_APPLICATION` é só valor de enum de auditoria. Como a USP-022 é a **primeira** que precisa contar candidaturas (E-003), ela **introduz a tabela `applications` mínima** (id, candidatoId→Person, jobId→Job, `cancelledAt DateTime?` null=ativa, createdAt, `@@index([jobId, cancelledAt])`). O caminho de **escrita** (candidatar/cancelar), o índice único parcial de unicidade e o vínculo de encaminhamento (FK `Referral`, model inexistente) ficam para **USP-025/044**. Detalhe filtra **on-read** reusando o `where` de `search-jobs.ts` (`ACTIVE AND validUntil>=hojeSP AND company.isVerified`) → retorna `null` se não casa (estado "vaga encerrada", E-005/P-004/P-005). `viewJobDetail` é a **única fonte de anonimização** (ADR-0022), consumida pela página **e** pelo `generateMetadata`/JSON-LD (P-002 em todos os canais). Contador só com N≥3 (`APPLICATION_COUNTER_THRESHOLD`, P-001).
**Reason:** O card aponta `applications (contador)` como schema da USP-022 (TD §4.5); contar sem a tabela seria fabricação. Mesmo padrão de AD-011 (USP-021 estendeu o `Job` que a USP-020 criou) — a US que precisa primeiro cria a infra mínima. O board `#173` (6h, single task) sub-especificou: faltavam schema, limiar do contador (P-001), anonimização de metadados/JSON-LD (P-002), estados não-ativos (E-005) e CTA "ativar candidato" (E-004).
**Trade-off:** Cresce de 6h (#173) para ~20h (4 tasks: schema applications, query+view, página+estados+CTAs, metadados). Épico #6 163h→~177h. USP-025 terá de **estender** `applications` (re-migração aceita e explícita). T3/T4 compartilham `page.tsx` → sequenciais.
**Impact:** Desbloqueia USP-025 (candidatar-se, downstream). Forma de `applications` definida aqui passa a vincular a USP-025. Spec/design/tasks em `.specs/features/vagas/usp-022-detalhe-vaga/`. **Status:** plano aguarda aprovação do Dev Sênior antes do Execute (ICE must-not).

### AD-011: USP-021 — estender o schema `Job` ao contrato canônico do TD §4.5 + busca `unaccent` (kickoff 2026-06-20)

**Decision:** USP-021 (#169) entrega os **6 filtros** dos expectations (E-002): área, escolaridade, tipo de contrato, regime, faixa de salário, região. O `Job` implementado na USP-020 divergiu do TD §4.5 (simplificou para `salary`/`location`/`workRegime` freetext, sem escolaridade/contrato/faixa-salário/região-FK/`salaryVisible`). Decisão do dono (2026-06-20): **estender o schema AGORA** — migração adicionando `educationLevelRequired?`, `contractType`, `salaryMin?`/`salaryMax?`/`salaryVisible`, `regionId`+relação `Region` (taxonomia já existe no schema), preservando colunas existentes/nullable (padrão rascunho da USP-020). Reabre o `JobForm` da USP-020 (#165) para coletar os novos campos. Busca textual sem acento (E-003) via **extensão Postgres `unaccent`** + índice funcional (GIN/trigram), filtrando no DB sobre `título+descrição+requisitos` (runbook-search-pagination). `salaryVisible=false` omite salário no View Model anônimo e autenticado.
**Reason:** O card aponta TD §4.5 como fonte; entregar só 4 filtros deixaria a USP fora do contrato. `unaccent` no DB respeita paginação obrigatória (não filtra em memória) e escala além do volume MVP (<30 vagas). Region já está modelada — só faltava a FK no Job.
**Trade-off:** Cresce além das 12h do board (migração + backfill + reabertura do form da USP-020). Estimativa revisada ~22h; board precisa de subtask extra (migração/seed) além de #170 (query+view) e #171 (UI). Vagas existentes (seed) precisam de backfill dos novos campos NOT NULL → tornar `contractType`/`regionId` opcionais ou backfillar no seed.
**Impact:** Marca o vetor de descoberta (suporte a MP6); desbloqueia USP-022 (detalhe) e USP-025 (candidatura). Anonimização no serializer (ADR-0022) cobre F1/P-001. Spec/design/tasks em `.specs/features/vagas/usp-021-buscar-vagas-publica/`.

### AD-010: USP-017 — verificação de Empresa reusa a infra já cabeada pela USP-016 (kickoff 2026-06-19)

**Decision:** USP-017 (#155) NÃO cria caminho novo de efeito colateral — o `transitionContent` já chama `COMPANY_VERIFY_HOOK_TOKEN.onContentActivated(tx, …)` dentro do `tx` quando `to=ACTIVE` (`moderation/actions/transition-content.ts:106-113`, legado da USP-016/GAP-4), o evento `COMPANY_VERIFIED` já existe (`audit/events.ts:46`), `Company.isVerified` já existe e o DTO da fila já tem `companyUnverified?`. O trabalho real: (#156) migração com `verifiedAt`/`verifiedByPersonId`/`verificationJobId`/`verifiedSnapshot Json`/`rejectionCount`, adapter real `PrismaCompanyVerifyHook` (substitui o stub), idempotência via `isVerified=false` (AD-2, sem contar jobs), snapshot lido **dentro do tx** (P-004), incremento de rejeição (AD-5), guard P-005; (#157) painel+checklist+histórico de rejeições+diff de edição. Atomicidade aprovação-vaga↔verificação por ADR-0024. Spec/design/tasks/facts em `.specs/features/moderacao-conteudo/usp-017-validar-empresa-primeira-vaga/`.
**Reason:** A USP-016 deixou o hook como stub explicitamente para a USP-017 (GAP-4). Detectar "1ª vaga" pela própria flag `isVerified` (não por `count(jobs)`) é mais barato e correto sob rebaixamento da USP-015 (volta a `false`). Histórico de rejeições vive no `audit_log` (ADR-0023) — sem tabela nova; `rejectionCount` é só o agregado p/ o badge.
**Trade-off:** Caminho de rejeição pode exigir estender o hook com `onContentRejected` (R2/AD-5) — confirmar em #156.
**Impact:** Marca MP2; desbloqueia exibição de Empresa verificada (USP-027/028/041). Entry gate de DEV aberto; D-001 (conteúdo da checklist) é gate só de produção (ver B-004).

### AD-009: USP-020 — `Job` segue padrão `CandidateProfile` (status na entidade), não `content_items` do TD §4.5 (2026-06-16)

**Decision:** O model `Job` (USP-020 / #162) tem coluna `status ContentStatus @default(DRAFT)` na própria tabela (espelhando `CandidateProfile.publicationStatus`), **NÃO** as tabelas `content_items`/`content_transitions` do TD §4.5 — que **nunca foram implementadas** (USP-009 já divergiu; histórico vive em `audit_log`, ADR-0023). A FSM (`@/modules/moderation`) já suporta `ContentKind.JOB` e `TRANSITIONS[JOB]` (inclui `DRAFT→IN_MODERATION`); falta só registrar um `PrismaJobStatusRepository` no `byKind` do `DispatchingContentStatusRepository` (parte de #164). Dedup exata (P-003/ADR-0021) via índice parcial SQL `WHERE status IN (estados vivos)`. `JobArea`/`Region`/`ContentStatus` já existem (taxonomia US #111). Spec/design/tasks em `.specs/features/vagas/usp-020-publicar-vaga/`.
**Reason:** O schema implementado é a fonte da verdade sobre o TD doc (que descreve o supertipo `content_items` abandonado). Replicar o padrão `CandidateProfile` mantém coerência com o 1º conteúdo real que aterrissou na FSM.
**Trade-off:** Cada conteúdo (Job, Service) carrega seu próprio adapter de status + registro no container — duplicação controlada, mas a FSM/transições permanecem centralizadas.
**Impact:** Vale para todas as USPs de vagas downstream (021–024) e para serviços (029+): mesmo padrão de adapter por `ContentKind`. Fronteira da USP-020 = rascunho + submit→`IN_MODERATION`; verificação atômica da 1ª vaga (P-001) e filtro on-read (P-002/P-007) são USP-016/017/021/024.

### AD-008: USP-014 — remoção reusa `revokedAt`/`revokedBy` + nova coluna `revokeReason`; entrega em PR único (2026-06-16)

**Decision:** A remoção de responsável (USP-014) reusa `revokedAt`/`revokedBy` (não cria `endedAt`/`endedBy` como o body da #135 sugere — `schema.prisma:369-389`, comentário em `:338`). O **motivo** opcional vira **coluna de negócio `revokeReason String?`** no `PersonCompanyGrant` (migração pequena, sem backfill), NÃO um campo no audit_log. O `audit_log` registra o **evento** `COMPANY_RESPONSIBLE_REMOVED` (já catalogado — `audit/events.ts:49,125`). Invariante "≥1 ACTIVE" via regra pura `wouldLeaveCompanyWithoutResponsible` em `domain/grants.ts` (espelha `PrismaCompanyResponsibilityAdapter`). **Toda a US num único PR** (`feat/usp-014-remover-responsavel`) fechando #135 e #137.
**Reason:** O motivo é **atributo de negócio** do ciclo do vínculo (consumível por `reporting`), ao lado de quando/quem. Guardá-lo só no audit_log inverteria a dependência (reporting → auditoria forense) e exigiria extração de JSON numa tabela de alto volume — decisão revista a pedido do usuário (impacto em relatórios/consultas). Coluna nullable é estável, indexável e mantém relatórios desacoplados da auditoria. "Evitar migração" era argumento fraco — o projeto migra de rotina.
**Trade-off:** +1 coluna e +1 migração vs. a proposta inicial; em troca, motivo consultável por `select` e modelo coerente (quando/quem/porquê na mesma linha). Auto-remoção não invalida sessão: ADR-0030 revalida permissão por requisição (acesso cai na próxima navegação).

### AD-007: USP-013 — tabela `Outbox` mínima nesta USP; dispatcher fica na USP-044 (2026-06-15)

**Decision:** O design (ADR-0020/TD §4.6) pede `tx.outbox.create()` para o e-mail de aceite (E-003), mas não existe tabela outbox no código (nem a USP-012 envia e-mail). Criei a model Prisma **`Outbox`** mínima (`topic`/`payload`/`processedAt`/`attempts`/`lastError`) + migration, e `adicionarResponsavel` **enfileira** a linha na mesma transação. O **envio assíncrono real** (dispatcher que consome a fila e chama o `EmailSender`) é da **USP-044** — não foi implementado aqui.
**Reason:** É o que o design literalmente especifica (`tx.outbox.create()`), satisfaz o fact E-003 ("e-mail enfileirado") e respeita a atomicidade do ADR-0020, sem invadir o escopo de envio da USP-044. A pergunta foi levada ao usuário; o tool de decisão falhou tecnicamente e o usuário pediu "continue" → adotado o default mais fiel ao design e de menor risco.
**Trade-off:** O e-mail de aceite não é efetivamente enviado até a USP-044 entregar o dispatcher; no MVP a Pessoa aceita pelo painel (T6). Sem e-mail cadastrado, o vínculo PENDING é criado mas nenhuma linha de outbox é enfileirada.
**Impact:** USP-044 deve consumir `topic='email'` do `Outbox`, despachar via `ResendEmailSender`, marcar `processedAt`/`attempts`/`lastError`, e cobrir retentativa.

### AD-006: USP-013 — modelo pendente+aceite (não criação imediata) (2026-06-12)

**Decision:** O vínculo Pessoa↔Empresa criado por `adicionarResponsavel` nasce **`PENDING`** e só vira **`ACTIVE`** após **aceite explícito** da Pessoa adicionada (`aceitarVinculoResponsavel`). Busca por CPF/e-mail é **binária sem PII** (P-001), restrita a responsável ativo (P-005), com rate limit anti-enumeração (L-002). Schema: adicionar enum `CompanyGrantStatus (PENDING|ACTIVE)` + `pendingAt`/`acceptedAt` ao `PersonCompanyGrant` (hoje append-only sem status) + UNIQUE parcial `WHERE revoked_at IS NULL`. Sub-issues #130/#131 **expandidas** (sem nova sub-issue) para cobrir o fluxo de aceite (action + UI + e-mail).
**Reason:** As ACs originais do PRD/issue ("criação imediata") são vetor LGPD (fracasso de resultado F2 — adição não consentida). A fonte da verdade ICE (intent/expectations + technical-design §4.4/4.5/4.6) já resolveu isso exigindo aceite explícito (P-002). ICE > redação do PRD.
**Trade-off:** Mais escopo que as 2 sub-issues originais previam (action de aceite + UI de aceite + template de e-mail); PRs maiores. `createCompany` (USP-012) passa a gravar `status: ACTIVE` e a invariante "≥1 responsável ativo" conta só `ACTIVE`.
**Impact:** T1 (migration) toca USP-012 e `companiesLeftWithoutResponsible`. Gate jurídico D-001 vira pré-condição de deploy (B-003).

### AD-005: USP-016 — GAP-8 (acesso a status via port + fixture) e GAP-5 (alerta de fila diferido) (2026-06-10)

**Decision:** (1) **GAP-8** — como nenhum model real de conteúdo (`Job`/`Service`/`CandidateProfile`) existe ainda (só `Company`), `transitionContent` e a fila acessam a coluna `status` atrás do port `ContentStatusRepository`, com um adapter Prisma sobre a tabela transitória `_moderation_fixture` (`_`-prefix de infra, cf. `_health_check`) — o "1º tipo a aterrissar". Cada conteúdo real assume seu adapter na própria USP. (2) **GAP-5** — o alerta operacional de fila (>10 pendentes ou item >48h, E-005/P-001) fica **diferido**: sem SLA formal no MVP (TD §8.3).
**Reason:** Entregar a fundação `moderation` agora (destrava USP-009/017/018) sem criar models de conteúdo prematuros que colidiriam com as migrations das USPs donas. O alerta de fila não tem requisito de SLA no MVP.
**Trade-off:** A fila e `transitionContent` operam hoje sobre `_moderation_fixture` (vazio em produção até as USPs de conteúdo popularem via submissão); os adapters reais por tipo são trabalho futuro. Atomicidade real (status+audit na mesma tx) é exercitada nos testes de integração contra a fixture.
**Impact:** Ao desenvolver Job/Service/CandidateProfile, registrar o adapter concreto de cada tipo no `ContentStatusRepository` (despacho por `ContentKind`) e migrar a fila para unir as fontes reais; remover/aposentar `_moderation_fixture`. GAP-3 (e-mail real, USP-044) e GAP-4 (flag `isVerified`, USP-017) trocam os stubs no-op pelos adapters reais.

### AD-004: USP-009 pausada (só kickoff) até a USP-016 concluir (2026-06-10)

**Decision:** A USP-009 (#31) teve **apenas kickoff** (timer = 1.0h, parado). O desenvolvimento da USP-016 (#117) ocorre **antes**; a USP-009 só é retomada após a 016 estar concluída.
**Reason:** A USP-016 entrega `moderation.transitionContent()` + a máquina de estados — exatamente o GAP-1 (bloqueio) do #44 da USP-009. Desenvolver a 016 primeiro destrava a 009 naturalmente.
**Trade-off:** Troca de contexto; o timer da #31 foi parado para não poluir horas/tokens da 009 enquanto se trabalha na 016.
**Impact:** Ao retomar a 009, reiniciar o timer da #31; o kickoff (1.0h) está preservado no comentário da issue.

### AD-001: Estrutura spec-driven derivada da documentação existente (2026-05-23)

**Decision:** Criar `.specs/` a partir dos docs em `docs/` (PRD `prd-asonseg-portal-mvp.md`, `docs/arch/*`, protótipo). 13 épicos → 13 features; faseamento da arquitetura (Fase 0–6 + Lançamento) → milestones do ROADMAP.
**Reason:** Projeto greenfield (sem código) com documentação rica e canônica já existente — o spec-driven formaliza o WHAT/HOW rastreável sobre essa base.
**Trade-off:** Specs por épico geram alguma sobreposição com o PRD; mitigado mantendo specs focadas em ACs testáveis (WHEN/THEN/SHALL) + IDs rastreáveis, não em re-narrar o PRD.
**Impact:** Design.md e tasks.md NÃO foram gerados em massa — serão criados por feature no momento de execução (auto-sizing), quando TESTING.md e decisões de ferramentas existirem.

### AD-003: GitHub Project #3 populado com hierarquia Épico→US→Task (2026-05-23)

**Decision:** Board `DNACod3/asonseg-portal` (project #3) populado a partir do spec-driven: 8 Épicos (1 por fase, #4–#11), 55 User Stories (44 USP do PRD + 11 sintéticas: 4 infra Fase 0, 1 auditoria Fase 1, 2 hardening/LGPD Fase 6, 4 lançamento), 155 Tasks (PRs por camada do módulo). Issue types nativos da org (Epic/User Story/Task), sub-issues para hierarquia, Status=Backlog, Estimate em horas com rollup US=Σtasks e Épico=ΣUS (total **805h**).
**Reason:** Pedido do usuário; alinha o backlog executável ao protocolo OpenWolf (cascade Task→US→Épico).
**Trade-off:** `gh project` (GraphQL) tem rate limit baixo; `set_estimate` via `item-list --limit 800` é caro (~247 pts) — rollup foi centralizado em 1 passada (`/tmp/wolf_rollup.py`) em vez de por-agente.
**Impact:** Cada Task tem corpo agent-ready (objetivo, arquivos, passos, padrões, DoD, testes, commit). Tasks ainda em Backlog; ao iniciar uma, aplicar o protocolo `openwolf-task-protocol-asonseg-portal` (timer + Status In Progress).

### AD-002: Escopo do spec-driven = Portal (Release 1), não Frente 4 (2026-05-23)

**Decision:** O spec-driven cobre o Portal de Empregabilidade e Serviços (`prd-asonseg-portal-mvp.md`). Os PRDs/ADRs de `docs/prd/` sobre beneficiários, famílias e fito (`prd-asonseg-frente4-v2.md`, `prd-asonseg-mvp.md`) são Release 2 e ficam em Future Considerations.
**Reason:** CLAUDE.md define o Portal como o projeto canônico; os 11 módulos de domínio mapeiam ao Portal.
**Trade-off:** A fundação compartilhada (Pessoa unificada, LGPD, auditoria) entregue no MVP serve aos dois releases — registrado em PROJECT.md como meta.
**Impact:** Papéis BENEFICIARY/FAMILY_RESPONSIBLE existem no enum mas estão fora do escopo de implementação do MVP.

---

## Active Blockers

### B-001: DPO não designado (bloqueava go-live) — ✅ RESOLVIDO (2026-07-10, AD-024)

**Discovered:** 2026-05-23 (D-001 do PRD)
**Impact:** LGPD exige DPO/encarregado designado antes do go-live; bloqueava o Lançamento, não o desenvolvimento.
**Resolved:** 2026-07-10 — o dono designou o **DPO = a Diretora Geral** (o cargo, quem quer que o ocupe). Resta a **formalização escrita + publicação do contato do encarregado** (tarefa operacional/jurídica antes do cutover, não bloqueia dev nem merge).
**Follow-up ligado:** retenção de PII (5 anos) + direito ao esquecimento seguem como unidade **pós-piloto** (AD-024).

### B-002: `moderation.transitionContent()` inexistente bloqueava USP-009 #44 — ✅ RESOLVIDO (2026-06-10)

**Discovered:** 2026-06-10 (GAP-1 do design da USP-009)
**Resolved:** 2026-06-10 — USP-016 mergeada entregou `@/modules/moderation` (`transitionContent`, `ContentKind`, `ContentStatus`, `ContentStatusRepository`). O trio da USP-009 foi atualizado (spec/design/tasks/tests).
**Trabalho herdado pela #44 (não é mais bloqueio, é integração — ver AD-005):** adicionar `ContentKind.CANDIDATE_PROFILE` + transições; adapter `PrismaCandidateProfileStatusRepository`; despacho por `ContentKind` no `container.ts`. Resta GAP-2 (evento `CANDIDATE_ROLE_ACTIVATED`) — a submissão já é auditada por `transitionContent` (`CONTENT_SUBMITTED_TO_MODERATION`).

---

### B-003: Gate jurídico D-001 da USP-013 (bloqueia deploy, não merge)

**Discovered:** 2026-06-12 (D-001 de `expectations-USP-013.md`)
**Impact:** Antes da USP-013 ir para **produção**, diretoria + jurídico devem decidir **por escrito** o modelo de aceite (explícito por aceite no painel vs. notificação a posteriori). O design já implementa "aceite explícito" (AD-006), então é confirmação formal, não mudança técnica. Não bloqueia desenvolvimento nem merge.
**Workaround:** Desenvolver e mergear normalmente o modelo pendente+aceite; obter o sign-off antes do cutover/deploy da feature.
**Resolution:** Diretoria/jurídico assinam a decisão do modelo de aceite.

---

### B-004: Gate de produção D-001 da USP-017 — conteúdo da checklist de verificação (bloqueia go-live, não merge)

**Discovered:** 2026-06-19 (D-001 de `expectations-USP-017.md`, kickoff #155)
**Impact:** Antes da USP-017 ir para **produção**, a **checklist de verificação de Empresa** (critérios objetivos que o coordenador segue p/ aprovar/rejeitar) precisa estar validada **por escrito** (sponsor + coordenador + Bravi PO) e testada com voluntários. Sem ela, RP-005 (empresa-fantasma) fica desprotegido. É o entregável de **Fase 0** `seed-taxonomia-checklists` (AC-111-2).
**Workaround:** Desenvolver/mergear normalmente o **mecanismo** da checklist (P-001 RESOLVIDO — itens marcáveis + dispensa com motivo), lendo os itens de fonte configurável (seed), não hard-coded (R3 do design). O conteúdo é seedado depois sem redeploy.
**Resolution:** Sponsor+coordenador+Bravi PO assinam os itens da checklist; seed populado antes do cutover.

---

## Lessons Learned

_(nenhuma registrada ainda)_

---

## Quick Tasks Completed

| #   | Description | Date | Commit | Status |
| --- | ----------- | ---- | ------ | ------ |

---

## Deferred Ideas

- [ ] Triagem de moderação assistida por LLM (reduzir carga de moderação humana — risco RP-004) — Captured during: bootstrap, mitigação V2
- [ ] Kanban de status de candidatura — Captured during: bootstrap (fora do MVP)
- [ ] Busca semântica/FTS de vagas e serviços — Captured during: bootstrap (MVP usa match exato)

---

## Todos

- [ ] Definir TESTING.md (tipos de teste, comandos de gate, matriz de cobertura) antes de criar tasks.md de qualquer feature — o stack de teste já está definido (Vitest + Playwright) mas a matriz por camada/módulo precisa ser formalizada.
- [ ] Confirmar metas absolutas dos indicadores MP1–MP10 com o sponsor (QP-007 / D-004).
- [ ] Ao iniciar cada feature: gerar design.md (se Large/Complex) e tasks.md, e atualizar status no ROADMAP (PLANNED → IN PROGRESS).

---

## Preferences

**Model Guidance Shown:** never
